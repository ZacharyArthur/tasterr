## 0. Plex Contract and Security Gate

- [x] 0.1 Run a redacted household Plex spike for the current PIN-issued token against account identity, resource paging behavior, deterministic PMS/connection ordering, publicly valid HTTPS certificates with standard verification, machine identity, owner/managed/shared history scoping and per-row account ids, history pagination/order, account-scoped Continue Watching, its last-viewed/progress fields, and canonical movie/show TMDB GUID resolution; record only safe shapes and versions in `docs/PLEX-V2-SPIKE.md`.
- [x] 0.2 Reconcile every spike result with the proposal/design/specs before production code, explicitly resolving account-id scoping, managed/shared-user and Continue Watching isolation, connection allowlisting/TLS, never-followed redirects, GUID expansion, whether resource pagination exists, merge timestamps, legacy-token/JWT viability, and multi-server partial failure; stop if user isolation or verified TLS cannot be proved.

## 1. Typed Plex Read Boundary

- [x] 1.1 Extend the Plex wire client with typed cloud-account/resource/connection and PMS-local-account models, header-only credentials, owned-first/machine-id and local/direct/relay/URI ordering, at-most-four server selection, at-most-six connection attempts per resource, strict HTTPS validation with TLS verification left enabled, unauthenticated machine-identity verification, never-followed redirects, and generic upstream errors; cover valid, >4-server, >6-connection, malformed, hostile, invalid-certificate, redirect, timeout, token-leak, and multi-server fixtures.
- [x] 1.2 Add bounded PMS `/accounts`, history, nested Continue Watching hub, and metadata/GUID reads with fail-closed local-account resolution (including the live-proven explicit-403 self-only fallback), explicit account filtering and exact returned-row `accountID` validation, JSON negotiation, history pagination, no resource pagination, 500-row/server and 500-metadata/history-sync caps, 50-hub-item/server and 20-post-merge-resolution Continue Watching caps, concurrency eight, total deadlines, and invented fixtures for owner id mismatch, ambiguous account name, movie, episode-to-show, last-viewed duplicate merge, missing GUID/account id, account mismatch, invalid progress/context, and unexpected fields.
- [x] 1.3 Add the smallest Plex-to-catalog orchestration seam that maps canonical TMDB ids to existing summaries, proves Continue Watching is caller-scoped or skips it, generates episode context from validated integers, merges duplicate server results deterministically, caches only secret-free positive/negative results per user for five minutes without stale-on-error, and proves no Plex URL/token/server metadata crosses browser or persistence boundaries.
- [x] 1.4 Cancel and await paired account/discovery work after either side fails, and reject metadata whose echoed rating key differs from the requested identity.

## 2. Plex Watch-History Signals

- [x] 2.1 Add Alembic revision `0006` for nullable `users.plex_history_attempted_at`/`plex_history_synced_at` and the expanded unique-per-title signal index; on downgrade delete `watched_plex`, restore the prior index, drop both timestamps, and strip only v2 disabled-rail ids from settings. Cover fresh upgrade, existing data/settings preservation, v1.1-compatible downgrade, model/schema consistency, and no-fail handling of absent/unparseable/already-compatible settings rows or missing disabled-list keys.
- [x] 2.2 Add server-only `watched_plex` at +2.5 with latest-timestamp idempotent upsert, profile invalidation, strong-positive/engaged semantics, client rejection, episode-collapse behavior, and pure profile/store/scorer/API regression coverage; prove `seed_request_history`, `watchlist`, and `not_interested` retain their existing do-nothing-on-duplicate timestamp semantics.
- [x] 2.3 Add one attempt-gated Plex history path used by Plex login, Home, and reset after the existing Seerr seed settles. Use Plex-specific single-flight state that only reads the seed set; after task creation, commit the attempt timestamp in the task's own fresh session before network work. Enforce the 365-day window, 24-hour overlap, captured-cutoff success watermark, six-hour attempt throttle, newest-first caps, deadline-as-failure, all-network-before-batched-write behavior, and global partial-failure semantics. Cancel reset work with `asyncio.gather(..., return_exceptions=True)` or equivalent and test repeated failures, failed task creation/attempt commit, partial deadline expiry, >500-row backlog, seed/sync ordering, write-lock isolation, hung-network reset completion within 30 seconds, reset 200, and no post-reset commit.
- [x] 2.4 Select the newest non-hidden qualifying strong-positive signal as the related-title source; label it **Because You Watched X** only for `watched_plex` and **More Like X** otherwise. Cover mixed recency/kinds, hidden title, unavailable detail, and fallback.
- [x] 2.5 Cap the serial related-title fallback at the three newest unique eligible sources and prove a fourth source is not fetched.
- [x] 2.6 Cancel and await every per-server history/metadata child when an import is cancelled, and add an integrated real-import/reset regression proving a hung read is consumed, reset returns 200, and no Plex signal or timestamp reappears.
- [x] 2.7 Clear both Plex timestamps with an unconditional reset update and prove a concurrent attempt committed after request-session resolution cannot survive the reset or suppress its re-import.
- [x] 2.8 Pin signal conflicts to the intended partial unique-key shape, invalidate Plex-influenced profile caches on downgrade, and prove reset preserves another user's Plex timestamps.

## 3. Continue Watching Rail

- [x] 3.1 Register the default-enabled `continue-watching` rail type and add a caller-scoped, non-exclusive provider at the front of personalized response order without using the request DB session. Apply deterministic bounded multi-server merge, six connection attempts/resource, 50 hub items/server, 20 post-merge TMDB resolutions, a ten-second aggregate deadline, and five-minute secret-free per-user positive/negative caching without stale-on-error. Cover non-exclusive fetch overlap with stable response order, cross-rail de-duplication, minimum size, disabled/tokenless/unscoped/Plex-down degradation, hanging transport, shared-user cache hits, successful-to-negative and negative-to-recovered transitions, healthy cold-start rendering, and no-Seerr dependency.
- [x] 3.2 Add optional 1–99 `floor(100 * view_offset / duration)` progress and locally generated episode context once to `MediaSummary` (inherited by detail), regenerate OpenAPI/frontend types at this checkpoint, and render an accessible percentage label/bar on existing media cards without changing ordinary cards; omit zero/complete/missing/invalid items and cover boundary math, movie, episode-to-show, invalid coordinates, reduced-motion, keyboard/remote, availability hydration, and account-cache isolation.
- [x] 3.3 Validate each selected resource and its six bounded advertised connections concurrently while preserving deterministic resource/connection preference, using the redacted cold-path timing to tune the Continue Watching aggregate deadline to ten seconds; cover a healthy late-priority connection behind stalled siblings.
- [x] 3.4 Pin deterministic connection preference and pending-probe cancellation, cancel sibling server discovery after unexpected failure, and cap newest-first Continue Watching candidates at 20 before Plex metadata expansion while retaining the post-merge TMDB cap.
- [x] 3.5 Keep the decrypted account token masked in the rail context, reject invalid advertised URLs before applying the six eligible-probe budget, and start non-exclusive Home work before serial session-backed providers while retaining cancellation cleanup and response priority.
- [x] 3.6 Gate Continue Watching before account-token decryption and unwrap its masked token only inside the provider fetch.

## 4. Low-Correlation Exploration Rail

- [x] 4.1 Add a pure exploration ranker over a `CANDIDATE_CAP = 150` broad pool that excludes hidden/every strong-engaged title, rejects negative cosine, admits the lowest non-negative similarity quarter with deterministic ties, then ranks by shrunken quality plus the single availability boost and diversity; cover broad/narrow/uniform profiles, relative distinctness, ordering, negative-profile, duplicate, thin-pool, deterministic, and honest low-overlap explanation behavior.
- [x] 4.2 Register and compose the default-enabled `unexpected-picks` rail as **Picks You Wouldn't Usually Watch** after the principal personalized/trending/popular/recent rows, preserving earlier-rail priority, independent failure/disable behavior, profile-less omission, cross-user personalization, and catalog-source reuse when presentation source rails are disabled.
- [x] 4.3 Apply the approved Home sequence with Popular TV, Top Rated TV before services, services before decades, and all movie/TV genre rails mixed after decades in a stable per-user daily order; rotate the bounded related-title source order per user/day without changing candidate ranking.
- [x] 4.4 Preserve initial-Home cross-rail priority de-duplication but make paginated extra rails de-duplicate only within each rail, so overlapping providers do not under-fill according to arbitrary four-provider cursor boundaries.

## 5. Household Blend Rail

- [x] 5.1 Add session-gated household-member and same-origin/rate-limited blend APIs with explicit bounded models, caller included in ascending-local-id member results, `has_taste_signals` defined as at least one signal, caller-inclusive unique 2–6 validation, global rail gate, generic errors, and mutation inventory; regenerate OpenAPI/frontend types at this checkpoint and cover unauthenticated POST, deterministic ordering, stable eligibility across profile invalidation, and identity-field allowlisting.
- [x] 5.2 Implement at-most-six profile materializations with whole-request rejection on an empty selected profile, normalized mean blending, deterministic candidate union capped at 150, any-member hidden/engaged veto, and existing quality/availability/diversity ranking; update the taste-store module privacy docstring for authorized caller-inclusive cross-user blend reads and cover caller isolation, caller-without-taste rejection, empty profiles, score order, vetoes, caps, concurrent calls, generic/log-private failures, and TMDB/storage degradation.
- [x] 5.3 Add an inline, non-blocking, keyboard/remote-accessible audience picker and dynamic **Something for Everyone Tonight** rail with ephemeral state, caller locked-in, explicit submit, programmatic accessible names, nested availability hydration, generic announced failure, disabled/thin omission, confirmed-account-boundary reset, internal de-duplication, and the explicit allowance for repeating an already-visible Home title.
- [x] 5.4 Make the household picker a native disclosure that starts collapsed and hides/reveals its controls and result without disturbing Home or persisting new state.

## 6. Contracts, Documentation, and Review Evidence

- [x] 6.1 Run the final OpenAPI/frontend-type drift check; extend PublicConfig, explicit-response-model, outbound-import, mutation-inventory, fixture-secret, logging, account-row isolation, and browser cache-isolation regressions for all v2 paths.
- [x] 6.2 Update `README.md`, `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md`, and `docs/SECURITY.md` (including the exact Plex advertised-URL/redirect/TLS checklist), relevant deferred/ignored decisions, and operator/live-test instructions for Plex-aware behavior, privacy/degradation, legacy-agent TMDB-GUID coverage/remedy, dead-server removal by unsharing it in Plex, supported downgrade order, no-new-config operation, and the separate v2.0 release step without editing the frozen PRD/SPEC.
- [x] 6.3 Add opt-in live Plex contract tests for token validity, resource paging behavior/selection, verified TLS plus machine identity, PMS-local account resolution, per-row account-filtered history paging, owner/managed/shared and Continue Watching isolation, merge timestamp, and GUID mapping; run them with operator-supplied credentials, record redacted versions/results, and keep default/CI runs network-free.
- [x] 6.4 Review each implementation checkpoint for KISS, privacy, SSRF/redirect safety, bounded work, no new dependency, and exact living-spec coverage; run strict OpenSpec validation and fix every artifact issue.
- [x] 6.5 Pin the exact three v2 rail ids and labels returned by the settings API.

## 7. Integrated V2 Verification

- [x] 7.1 In the isolated LAN stack, verify owner plus available managed/shared/local-login cases, multi-user history separation, reset/re-import, healthy first-load Continue Watching rendering, an unexpected-picks rail with at least four post-de-duplication items for a realistic profile, all three rail toggles, Plex/one-server/Seerr/TMDB failure degradation, 1280×720 and 1920×1080 keyboard/remote flows, and absence of tokens/private upstream data in browser/network/log output.

## 8. Quality Gate

- [x] 8.1 Run `just check` inside the devcontainer and fix every backend/frontend failure.
